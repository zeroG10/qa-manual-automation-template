# Shared Requirements

Functional and non-functional requirements that apply **regardless of platform**:

- Business logic (independent of UI)
- API contracts
- Data models
- Cross-platform NFRs (security, compliance, SLAs)

Platform-specific requirements go under `../web/` or `../mobile/`.
